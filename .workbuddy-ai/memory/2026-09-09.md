# 2026-09-09

## Reverted Blender damage
- Overwrote `assets/goatchan/goatchan.blend` (23.8MB → 14.8MB) and `goatchan.glb` with a too-aggressive fix2.py (crude bbox distance reweighting, hand vert scaling). User said the head/body got fked up.
- **Reverted**:
  - `.blend` from `E:/Downloads/ReleaseVer/goatchan.blend` (original 23819900 bytes)
  - `.glb` from `git checkout 3c91f74 -- assets/goatchan/goatchan.glb` (original 20204232 bytes)
  - Reverted my `足先` rig-map addition in glbModel.js
- Files: assets/goatchan/goatchan.{blend,glb} now match originals byte-for-byte; glbModel.js is back to the pre-edit state.

## Lesson
- NEVER mass-reweight or scale vertices in Blender without a verified safe baseline.
- ALWAYS work on a `.blend` copy (`bpy.ops.wm.read_factory_settings()` + import .blend) and never `save_as_mainfile` over the original.
- NEVER touch `.blend` while iterating — fix the GLB/JS path instead.

## ReleaseVer inventory
- `E:/Downloads/ReleaseVer/` = goatchan release package, contains:
  - `goatchan.blend` (original, 23.8MB)
  - `tex/` (8 textures — body/face/hair)
  - `config/config_XXXX.uni` (165 frames, gzipped configs, ~95B each)
  - `mesh/fluid_mesh_XXXX.bobj.gz` (165 frames, ~5KB gzipped binary OBJ surface meshes)
  - `data/fluid_data_XXXX.vdb` (165 frames, ~600KB OpenVDB density fields)
  - This is a **baked fluid simulation** (likely ejaculate/liquid) — 165 frames from the original game. Could feed a real-time fluid playback for climax.
- No README/docs. Frame range: 0090 → 0254.

## Active problem (current state)
- Goat-chan model reverts to **pigeon-toed V-feet** + floating feet in solo pose.
- The 足捩 (foot-twist) bones and つま先 (toe) bones are present in the original GLB.
- The pigeon-toe is a **runtime IK/ankle rest tuning** problem, not a mesh problem.
- Look at `js3d/glbModel.js` GLB_ANKLE0, GLB_ANKLE_K, GLB_DANGLE_K + the foot IK in `js3d/anim3d.js`.

## Foot fix (runtime tuning only — no model edit)
- Hoisted GLB_ANKLE0/K and dangleK out of glbDriveRig into module-level
  `GLB_FOOT_TUNE` object so verify runs can sweep live.
- Added `toeK` (default 0.5) and `toeAbs` (default true) to the same object.
- Set `ankle0 = -0.6` (was -1.6). Empirically swept; -0.6 puts the toe at
  ~+0.7cm above the ankle (natural relaxed foot on bed) vs the old -1.6
  which lifted toes +8.5cm (the "toes up" look).
- Set `toeAbs = true`. The MMD つま先 bind has Z ≈ ±π (the bone's local
  frame is authored flipped 180°). Adding onto that bind folded the
  toe under the sole; absolute mode replaces the bind so the toe now
  tracks the ankle's plantarflexion (toeR goes from 2.67,-3.14 → 0.05,0).
- Files touched: js3d/glbModel.js only. Model files unchanged (still match
  original SHA via `git status assets/goatchan/`).

## Verify harness findings
- 19 rig bones resolve correctly via cleaned-key lookup (足L, ひざL, 足首L,
  つま先L, etc — Three.js GLTFLoader strips the dot).
- her3 procedural rig is correct: legL.foot at X=-0.305, legR.foot at X=+0.32
  (splayed outward, NOT crossed). The "X-cross" was a misread — was the
  visible mesh getting correct bones.
- The real foot issue was toe-bone over-rotation (2.67 rad + 180° Z flip).
  Fixed via toeAbs.
